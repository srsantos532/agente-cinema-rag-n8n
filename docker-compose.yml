version: '3.8'

services:
  n8n:
    image: n8nio/n8n
    container_name: n8n_final
    restart: always
    ports:
      - "5678:5678"
    environment:
      - N8N_SECURE_COOKIE=false
      - WEBHOOK_URL=http://163.176.33.131:5678/
    volumes:
      - ~/.n8n:/home/node/.n8n
